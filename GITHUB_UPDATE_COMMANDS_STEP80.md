# GitHub update commands for Step80

Run these from the project root after replacing your old files with this package.

```powershell
git status
git add .
git commit -m "Step80 stabilize crypto ai system"
git push origin main
```

If the remote is not set:

```powershell
git remote add origin https://github.com/thomasryu1992-commits/crypto_ai_system.git
git branch -M main
git push -u origin main
```
