# GitHub Update Commands - Step150

```powershell
cd crypto_ai_system
python run_step150_validation.py
git add .
git commit -m "Step150 spreadsheet-first guarded crypto ai system"
git push origin main
```

If remote is not configured:

```powershell
git remote add origin https://github.com/thomasryu1992-commits/crypto_ai_system.git
git branch -M main
git push -u origin main
```
