# GitHub Update Commands - Step120

```powershell
cd crypto_ai_system
python run_step120_validation.py
git add .
git commit -m "Step120 dry-run paper and live-shadow guarded system"
git push origin main
```
