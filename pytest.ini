[pytest]
pythonpath =
    src
    .
testpaths =
    tests
python_files =
    test_*.py
norecursedirs =
    .review
    dist
markers =
    integration: exercises the real pipeline end to end (touches storage)
